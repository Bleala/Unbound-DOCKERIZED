name: Unbound Deployment
run-name: Unbound Deployment For Production Usage 🚀
on:
  push:
    tags:
      - "*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: 'Specify the version to build and tag (default: 1.23.0)'
        required: true
        default: '1.23.0'
        type: string
      branch:
        description: 'Specify the branch to build from (default: main)'
        required: true
        default: 'main'
        type: string 

env:
  IMAGE_NAME: unbound
  REGISTRY_DOCKERHUB: docker.io
  REGISTRY_GHCR: ghcr.io
  REGISTRY_GITEA: ${{ secrets.LOCAL_URL }}
  USERNAME_DOCKERHUB: ${{ secrets.DOCKERHUB_USERNAME }}
  USERNAME_GHCR: ${{ secrets.GHCR_USERNAME }}
  USERNAME_GITEA: ${{ secrets.LOCAL_USERNAME }}

jobs:
  Unbound-Deployment-Production:
    name: Unbound Deployment Production
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      # https://github.com/marketplace/actions/checkout
      - name: Check Out Repository Code
        id: checkout
        uses: actions/checkout@v4.2.2

      # https://github.com/marketplace/actions/hadolint-action
      - name: Lint Dockerfile
        id: dockerfile-linter
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: ./docker/Dockerfile

      # https://github.com/marketplace/actions/shell-linter
      - name: Lint Shell Scripts
        id: shell-linter
        uses: Azbagheri/shell-linter@v0.8.0
        with:
          exclude-paths: "LICENSE,docker/Dockerfile"

      # https://github.com/marketplace/actions/docker-hub-description
      - name: Docker Hub Description
        id: dockerhub-description
        uses: peter-evans/dockerhub-description@v4.0.2
        with:
          username: ${{ env.USERNAME_DOCKERHUB }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.USERNAME_DOCKERHUB }}/${{ env.IMAGE_NAME }}
          short-description: "Unbound - a validating, recursive, and caching DNS resolver. DOCKERIZED!"

      # https://github.com/marketplace/actions/docker-metadata-action
      - name: Docker Metadata (Tagging)
        id: metadata
        uses: docker/metadata-action@v5.7.0
        with:
          # list of Docker images to use as base name for tags
          images: |
            ${{ env.REGISTRY_DOCKERHUB }}/${{ env.USERNAME_DOCKERHUB }}/${{ env.IMAGE_NAME }}
            ${{ env.REGISTRY_GHCR }}/${{ env.USERNAME_GHCR }}/${{ env.IMAGE_NAME }}
            ${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}
          flavor: |
            latest=true
          # generate Docker tags based on the following events/attributes
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}

      # https://github.com/marketplace/actions/docker-setup-qemu
      - name: Set Up QEMU
        id: setup-qemu
        uses: docker/setup-qemu-action@v3.6.0

      # https://github.com/marketplace/actions/docker-setup-buildx
      - name: Set Up Docker Buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@v3.10.0

      # https://github.com/marketplace/actions/docker-login
      - name: Login To Docker Hub
        id: docker-login-dockerhub
        uses: docker/login-action@v3.4.0
        with:
          registry: ${{ env.REGISTRY_DOCKERHUB }}
          username: ${{ env.USERNAME_DOCKERHUB }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # https://github.com/marketplace/actions/docker-login
      - name: Login To GitHub Container Registry
        id: docker-login-ghcr
        uses: docker/login-action@v3.4.0
        with:
          registry: ${{ env.REGISTRY_GHCR }}
          username: ${{ env.USERNAME_GHCR }}
          password: ${{ secrets.GHCR_TOKEN }}

      # https://github.com/marketplace/actions/docker-login
      - name: Login To Gitea
        id: docker-login-gitea
        uses: docker/login-action@v3.4.0
        with:
          registry: ${{ env.REGISTRY_GITEA }}
          username: ${{ env.USERNAME_GITEA }}
          password: ${{ secrets.LOCAL_TOKEN }}

      # https://github.com/marketplace/actions/build-and-push-docker-images
      - name: Build And Push Pre Release Image For Scanning
        id: build-and-push-pre-release
        uses: docker/build-push-action@v6.18.0
        with:
          context: ./docker
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          push: true
          tags: ${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:pre-release
          cache-from: type=registry,ref=${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

      # # Set up Tricy and scan pre-release-dev image from Registry
      # Normal GitHub Action does not work with Gitea Actions, because Trivy wants to login to GitHub
      # # https://github.com/marketplace/actions/aqua-security-trivy
      - name: Install Trivy And Scan Image For Vulnerabilities
        id: trivy-scan
        run: |
          # Schritt 1: Trivy-Binary installieren
          # Wir laden die neueste Version direkt herunter
          export TRIVY_VERSION=$(curl -s "https://api.github.com/repos/aquasecurity/trivy/releases/latest" | grep '"tag_name":' | sed -E 's/.*"v([^"]+)".*/\1/')
          echo "Installing Trivy version: $TRIVY_VERSION"
          curl -sL "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.deb" -o trivy.deb
          sudo dpkg -i trivy.deb

          # Schritt 2: Trivy-Scan mit exakt deinen gewünschten Parametern ausführen
          # Der Befehl ist: trivy image [optionen] [image-name]
          trivy image \
            --format table \
            --exit-code 1 \
            --ignore-unfixed \
            --pkg-types os,library \
            --severity CRITICAL,HIGH \
            ${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:pre-release

      # https://github.com/marketplace/actions/build-and-push-docker-images
      - name: Build And Push Final Image
        id: build-and-push-final
        uses: docker/build-push-action@v6.18.0
        with:
          context: ./docker
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          push: true
          provenance: false
          sbom: false
          tags: |
            ${{ steps.metadata.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

      # https://github.com/regclient/regclient
      - name: Clean Up 'pre-release' Image
        id: clean-up-pre-release
        if: success()
        run: |
          echo "Installing regctl."
          curl -sL "https://github.com/regclient/regclient/releases/latest/download/regctl-linux-amd64" -o regctl
          chmod +x ./regctl

          # regctl automatically uses the Docker Login credentials from the Docker Login Action
          echo "Deleting pre-release tag from Docker Hub."
          ./regctl tag rm ${{ env.REGISTRY_GITEA }}/${{ env.USERNAME_GITEA }}/${{ env.IMAGE_NAME }}:pre-release

          echo "Cleanup successful."

      # https://github.com/niniyas/ntfy-action
      - name: Ntfy Notification
        id: ntfy-notification
        uses: niniyas/ntfy-action@v1.0.4
        if: always()  # Wird immer ausgeführt, egal ob success, failure oder cancelled
        env:
          STATUS: ${{ job.status }}
        with:
          url: '${{ secrets.NTFY_URL }}'
          topic: '${{ secrets.NTFY_TOPIC }}'
          tags: ${{ env.STATUS == 'success' && '+1,partying_face,tada,action,successfully,completed' || env.STATUS == 'failure' && '-1,no_entry,skull,action,failed' || env.STATUS == 'cancelled' && '-1,cry,triangular_flag_on_post,action,cancelled' }}
          title: "Gitea Actions ${{ env.STATUS == 'success' && 'erfolgreich' || env.STATUS == 'failure' && 'fehlgeschlagen' || env.STATUS == 'cancelled' && 'abgebrochen' }}: ${{ env.GITHUB_REPOSITORY }} | ${{ env.GITHUB_REF_NAME }}"
          priority: ${{ env.STATUS == 'success' && 3 || 5 }}
          details: "Gitea Actions Workflow ${{ env.STATUS == 'success' && 'ist erfolgreich abgeschlossen.' || env.STATUS == 'failure' && 'ist fehlgeschlagen!' || env.STATUS == 'cancelled' && 'wurde abgebrochen!' }}"
          # Bearer Header notwendig in JSON --> https://docs.ntfy.sh/publish/#access-tokens
          headers: '${{ secrets.NTFY_HEADERS }}'
          actions: '[{"action": "view", "label": "Gitea öffnen", "url": "${{ secrets.GIT_URL }}/${{ env.GITHUB_REPOSITORY }}/actions", "clear": true}]'
          icon: 'https://w7.pngwing.com/pngs/940/571/png-transparent-gitea-hd-logo.png'
          image: true
